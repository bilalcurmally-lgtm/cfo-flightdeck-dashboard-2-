# Grok Brief — D2 Slice 4 Task 4.1 (welcome-back strip renderer)

S3 is merged (capture wiring + `ImportComparison` produced on import). Implement **Task 4.1
only** of `docs/superpowers/plans/2026-06-06-d2-import-history.md` — the pure strip renderer.
Follow the plan's exact test + code + styles.

## Scope
- Create `src/ui/welcome-back-strip.ts` — `renderWelcomeBackStrip(comparison, {formatMoney,
  formatRunway}) → string` (pure; reuse `escapeHtml` from `./html`).
- Create `src/ui/welcome-back-strip.test.ts` — the plan's 3 tests (summary clauses;
  omit-zero-clauses; olive/coral tone classes).
- Append the `.bw-welcome*` styles to `src/styles.css` (the plan's CSS block).
- **STOP after Task 4.1.** Do NOT touch `main.ts` — wiring the strip into the cockpit
  (Task 4.2) is claude-opus's. Do NOT touch `import-history.ts` or other modules.

## Hard rules
- Pure render only — input is an `ImportComparison` (import the type from
  `../workspace/import-history`); output is an HTML string. No DOM, no event binding.
- Markers the wiring depends on (keep exactly): `data-bw-welcome-strip` on the section,
  `data-bw-welcome-dismiss` on the dismiss button, and tone classes `bw-welcome--positive`
  / `bw-welcome--attention` / `bw-welcome--neutral`.
- TDD: failing test first. `npx tsc --noEmit` (0) + `npx vitest run src/ui/welcome-back-strip.test.ts`
  green. Then `npx vitest run` to confirm no regression. Golden signature literals untouched.
- One commit (the plan's message: `feat(ui): welcome-back strip renderer [D2]`). Push.

## When you hand back
Paste `tsc` + vitest counts and the commit hash. Ping claude-opus — I wire it into
`renderImportResult` (Task 4.2) + dismiss, then cut S5 (history panel renderer) for you.
