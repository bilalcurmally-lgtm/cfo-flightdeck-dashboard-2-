# Grok Brief — D2 Slice 2 (pure import-history logic)

S1 is merged (WorkspaceSnapshot v2 + `ImportSnapshot` type exist). Implement **Slice 2 only**
of `docs/superpowers/plans/2026-06-06-d2-import-history.md` — Tasks **2.1, 2.2, 2.3, 2.4**.
The plan has exact code/tests/commands/commit messages; follow it task-by-task (TDD).

## Scope (all in `src/workspace/import-history.ts` + `import-history.test.ts`)
- 2.1 — `recordImport` (append, dedup identical-to-most-recent signatureSet, cap at 24).
- 2.2 — `findComparableBaseline` (most recent prior snapshot sharing ≥1 signature).
- 2.3 — `diffKpiSnapshots` (per-key `{previous,current,delta,direction}`; null-safe; union of keys).
- 2.4 — `diffReviewSignatures` (added/resolved counts) + `compareToBaseline` (bundle).
- **STOP after Task 2.4.** No S3 (`addImport`/capture), no UI, no wiring.

## Hard rules
- Stay in `src/workspace/import-history.ts` (+ its test). Do NOT touch `main.ts`, `src/ui/`,
  `workspace-store.ts`, `project-file.ts`, `txn-signature.ts`, `sign-ledger.ts`, `ledger-diff.ts`.
- `import-history.ts` stays **dependency-free** — it only consumes `string[]` / its own types.
  Do NOT import `persistence-bridge`, DOM, or the store. (Per spec §9 locked decision.)
- TDD: failing test first. Per task: `npx tsc --noEmit` (0) + `npx vitest run src/workspace/`
  green. Commit per task (4 commits) with the plan's messages. Push when done.
- Golden signature literals untouched (you won't be near them, but confirm).

## When you hand back
Paste `tsc` + `vitest run src/workspace/` counts and the 4 commit hashes. Ping claude-opus.
I review, then take **S3** myself (the `addImport` accessor + `main.ts` capture wiring —
integration, my job). After S3 I'll cut S4 (strip renderer) for you.
