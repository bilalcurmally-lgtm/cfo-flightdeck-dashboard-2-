# Grok Brief — D2 Slice 1 (WorkspaceSnapshot v2 + migration)

Implement **Slice 1 only** of `docs/superpowers/plans/2026-06-06-d2-import-history.md` —
Tasks **1.1, 1.2, 1.3**. The plan has the exact code, tests, commands, and commit messages;
follow it task-by-task (TDD: failing test first, then implement, then green, then commit).

## Scope
- Task 1.1 — create `src/workspace/import-history.ts` with the `ImportSnapshot` type only.
- Task 1.2 — `WorkspaceSnapshot` v2 (`imports`) + load migration in `workspace-store.ts`.
- Task 1.3 — `project-file.ts` v1→v2 migrate (accept versions [1,2], migrate forward,
  reject newer/unknown + malformed `imports`).
- **STOP after Task 1.3.** Do NOT start S2 (`import-history` logic), S3, or any wiring.

## Hard rules
- Do NOT touch `src/main.ts`, `src/ui/`, `txn-signature.ts`, `sign-ledger.ts`, or
  `ledger-diff.ts`. Golden signature values stay byte-identical.
- **Full-suite gate (important):** S1 bumps `WORKSPACE_SNAPSHOT_VERSION` to 2, so run the
  WHOLE suite, not just `src/workspace/`: `npx tsc --noEmit` (0) AND `npx vitest run` (all
  green). If any pre-existing test asserts `version === 1`, update it to
  `WORKSPACE_SNAPSHOT_VERSION` (the plan's Task 1.2 Step 4 calls this out) — that's the one
  allowed edit to existing tests. If a test fails for any OTHER reason, STOP and report it;
  do not paper over it.
- Commit per task (3 commits) using the plan's messages. Push when done.

## When you hand back
Paste `tsc` + full `vitest run` output (counts) and the 3 commit hashes. Ping claude-opus —
I'll review (confirm migration correctness, golden values intact, full suite green) before
S2. S2's brief comes after S1 lands (S2 imports the v2 types).
